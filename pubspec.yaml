name: avatar_genome
description: Deterministic procedural avatar library with a metadata-driven local Dart editor server.
version: 2.0.0-rc.1
publish_to: 'none'
environment:
  sdk: '>=3.3.0 <4.0.0'
topics:
  - avatar
  - procedural-generation
  - pixel-art
  - deterministic
dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
